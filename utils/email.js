const nodemailer=require('nodemailer');

const sendEmail=options=>{

    //1)Create a transporter(which mode of transporter we are gonna use to transport email because the node will not send the email)


    //2)Define the email options


    //3)Actually send the email
}